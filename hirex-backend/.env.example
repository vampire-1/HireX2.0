# HireX Backend - Environment Variables Template
# Copy this file to .env and fill in your values

# Core Application Settings
# -------------------------
# Frontend URLs allowed to access the API (comma-separated)
FRONTEND_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

# Security
# --------
# Secret key for JWT tokens (use a strong random string in production)
SECRET_KEY=your-secret-key-here-change-in-production
JWT_SECRET=your-jwt-secret-here-change-in-production

# SMTP / Email Settings (Optional)
# --------------------------------
# Set to "true" to enable email verification
SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=HireX <no-reply@hirex.app>

# Database & Storage (Auto-configured for local development)
# ----------------------------------------------------------
# DATA_DIR=./data
# RESUME_DIR=./data/resumes
# DB_URL=sqlite:///./data/hirex.db

# Embeddings & Search (Auto-configured)
# ------------------------------------
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# FAISS_INDEX_PATH=./data/faiss_index.bin
# FAISS_META_PATH=./data/faiss_meta.jsonl

# OCR (Optional - for image-based resume parsing)
# -----------------------------------------------
# TESSERACT_CMD=/usr/bin/tesseract
# POPPLER_PATH=/usr/bin
